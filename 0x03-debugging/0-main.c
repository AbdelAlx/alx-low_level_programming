#include <stdio.h>

/**
 * Test_positive_or_negative - fuction that test the function positive_or_negative() and  gives the correct output when given a case of 0
 *
 * return - return 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
