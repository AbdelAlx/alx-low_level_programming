#include <stdio.h>

/**
 * _isupper - prints 1 or 0 according to the input
 * @c: input type variable
 *
 * Description: checks for uppercase letters
 * Return: Aways (0).
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
